machine:
  ruby:
    version: 2.4.0

test:
  override:
    - bundle exec rake

database:
  post:
    - createdb bearden_mockburden
    - createdb bearden_mockredshift

deployment:
  staging:
    branch: master
    heroku:
      appname: bearden-staging
  production:
    branch: release
    heroku:
      appname: bearden-production
